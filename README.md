# Mock Test App

1. Generate questions JSON from `mockTest.txt`:

```bash
npm run generate:questions
```

2. Install dependencies and run dev server:

```bash
npm install
npm run dev
```

App loads `src/data/mockQuestions.json` — run the generator again after updating `mockTest.txt`.
